import type { ResumeData } from "./types";
import { PremiumResumeTemplate } from "./premiumShared";

const Template7 = ({ data }: { data: ResumeData }) => (
  <PremiumResumeTemplate data={data} templateId={7} />
);

export default Template7;
