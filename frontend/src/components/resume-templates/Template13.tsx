import type { ResumeData } from "./types";
import { PremiumResumeTemplate } from "./premiumShared";

const Template13 = ({ data }: { data: ResumeData }) => (
  <PremiumResumeTemplate data={data} templateId={13} />
);

export default Template13;
