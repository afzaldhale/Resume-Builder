import type { ResumeData } from "./types";
import { PremiumResumeTemplate } from "./premiumShared";

const Template8 = ({ data }: { data: ResumeData }) => (
  <PremiumResumeTemplate data={data} templateId={8} />
);

export default Template8;
