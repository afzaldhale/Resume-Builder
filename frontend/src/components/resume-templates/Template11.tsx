import type { ResumeData } from "./types";
import { PremiumResumeTemplate } from "./premiumShared";

const Template11 = ({ data }: { data: ResumeData }) => (
  <PremiumResumeTemplate data={data} templateId={11} />
);

export default Template11;
