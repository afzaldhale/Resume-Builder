import type { ResumeData } from "./types";
import { PremiumResumeTemplate } from "./premiumShared";

const Template15 = ({ data }: { data: ResumeData }) => (
  <PremiumResumeTemplate data={data} templateId={15} />
);

export default Template15;
