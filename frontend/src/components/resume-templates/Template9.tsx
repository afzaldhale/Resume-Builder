import type { ResumeData } from "./types";
import { PremiumResumeTemplate } from "./premiumShared";

const Template9 = ({ data }: { data: ResumeData }) => (
  <PremiumResumeTemplate data={data} templateId={9} />
);

export default Template9;
