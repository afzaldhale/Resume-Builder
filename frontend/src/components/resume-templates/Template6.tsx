import type { ResumeData } from "./types";
import { PremiumResumeTemplate } from "./premiumShared";

const Template6 = ({ data }: { data: ResumeData }) => (
  <PremiumResumeTemplate data={data} templateId={6} />
);

export default Template6;
